services:
    backend-server:
        ports:
            - "6767:6767"
        build:
            context: .
            dockerfile: backend/server/Dockerfile

    backend-tools:
        build:
            context: .
            dockerfile: backend/tools/Dockerfile


