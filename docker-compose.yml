services:
    backend-server:
        ports:
            - "60000:6767"
        volumes:
            -   "./db:/app/db"
        build:
            context: .
            dockerfile: backend/server/Dockerfile

